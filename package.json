{
  "name": "payment-alipay",
  "version": "1.0.23",
  "description": "支付宝支付插件",
  "author": "01道",
  "license": "GPL",
  "keywords": [
    "payment-alipay"
  ],
  "scripts": {
    "clean": "cross-env ./node_modules/.bin/rimraf dist",
    "build": "cross-env npm run clean && tsc -p tsconfig.json",
    "watch": "cross-env tsc --watch",
    "test": "cross-env jest",
    "publish": "cross-env npm run build "
  },
  "dependencies": {
    "@medusajs/utils": "^1.11.3",
    "alipay-sdk": "^4.5.0"
  },
  "devDependencies": {
    "@medusajs/medusa": "^1.20.4",
    "@types/node": "^20.12.7",
    "cross-env": "^7.0.3",
    "rimraf": "^5.0.5",
    "typescript": "^4.4.4"
  },
  "peerDependencies": {
    "@medusajs/medusa": "^1.12.3"
  }
}
